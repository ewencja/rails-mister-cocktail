<%= simple_form_for(@cocktail) do |f| %>
<% unless @cocktail.persisted? %>
  <%= f.input :name %>
<% end %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit %>

<% end %>
